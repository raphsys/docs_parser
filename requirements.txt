# Target runtime: CPython 3.11 in the local `.docs-parser` venv.
# The pins below are aligned with the March 2026 handoff notes in `scripts/*.md`
# and with the packages imported directly by the project code.
# If pip resolves differently on your machine, install with:
#   python -m pip install -r requirements.txt -c constraints-py311.txt

# API / server
fastapi==0.135.2
python-multipart==0.0.22
uvicorn==0.42.0

# Core document parsing
numpy==2.2.6
opencv-python==4.13.0.92
Pillow==12.1.1
PyMuPDF==1.27.2.2
rapidocr-onnxruntime==1.2.3
requests==2.32.5
shapely==2.1.2

# Translation stack
ctranslate2==4.7.1
# Pinned from `scripts/session_handoff_2026-03-18.md` because a mismatch broke `transformers`.
huggingface_hub==0.36.2
PyYAML==6.0.2
sacremoses==0.1.1
sentencepiece==0.2.1
transformers==4.57.6

# Layout AI / PP-StructureV3 local pipeline
onnxruntime==1.24.4
paddlepaddle==3.2.0
paddlex==3.3.9
